apiVersion: v1
kind: Service
metadata:
  name: bincalc-service
  labels:
    app: bincalc
spec:
  type: LoadBalancer
  selector:
    app: bincalc
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http